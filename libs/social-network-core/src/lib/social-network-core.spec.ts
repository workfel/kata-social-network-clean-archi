import { socialNetworkCore } from './social-network-core';

describe('socialNetworkCore', () => {
  it('should work', () => {
    expect(socialNetworkCore()).toEqual('social-network-core');
  });
});
